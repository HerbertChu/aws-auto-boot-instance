# aws-auto-boot-instance
incase to the ec2 instance always running cost to much 
the script do the automation task to start or stop instance 
setup in aws lambda function &amp; it`s trigger by aws cloudwatch
